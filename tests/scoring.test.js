const { describe, it } = require('node:test');
const assert = require('node:assert');

const scoring = require('../src/scoring');

describe('scoring', () => {
  it('placeholder until aggregate() is implemented', () => {
    assert.ok(true);
  });
});
